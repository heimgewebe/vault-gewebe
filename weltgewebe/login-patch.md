Dialektische Erörterung (∴oszillation / ∴resonanz / ∴paradox) These: Public-Login soll „einfach funktionieren“: UI → Magic Link → Mail → Klick → Session. Dazu braucht es (a) Feature-Flag, (b) korrekte Base-URL, (c) Mailer, (d) Proxy/Origin/Cookie-Kohärenz, (e) Abuse-Schutz. Antithese: „Andere auch“ heißt: du öffnest einen Angriffs- und Spamkanal. Public Login ohne Rate-Limit + Observability + klare Sicherheitsdefaults ist wie ein Haustürschlüssel, den man an die Klingel klebt – mit dem Hinweis „bitte nicht klauen“. Synthese: PR bereitet Public Login so vor, dass er per Default sicher bleibt, aber sauber aktivierbar ist (env-gated), inkl. Minimal-Härtung (Rate-Limit, Audit-Logs, DX), plus klare Runbook-Schritte. PR-Erstellungs-Prompt (copy/paste für Agent) Du bist ein präziser Repo-Agent. Ziel: PR vorbereiten, damit Weltgewebe Public Magic-Link Login sauber unterstützt (ich + andere), inkl. Minimal-Sicherheit. Null-Interpolation: wenn Pfade/Dateien fehlen, erst suchen. Repo: weltgewebe (heimserver deployment). Branch: feat/public-login-ready Kontext: - UI zeigt "Public login is currently disabled." - Doku/Merge erwähnt AUTH_PUBLIC_LOGIN=1, APP_BASE_URL, AUTH_TRUSTED_PROXIES sowie Magic-Link-Flow und Log-basiertes Token (falls Mail fehlt). - Deployment läuft über docker compose (infra/compose/compose.prod.yml) mit .env. PR-Ziele (Definition of Done): 1) Public Login kann per ENV aktiviert werden: - AUTH_PUBLIC_LOGIN=1 ermöglicht /api/auth/login/request (oder äquivalent) via UI. - APP_BASE_URL wird konsistent genutzt, um Magic-Link-URLs korrekt zu bauen. - AUTH_TRUSTED_PROXIES wird unterstützt/ausgewertet (Forwarded / X-Forwarded-For). 2) Andere Nutzer können sich auch anmelden: - Registrierung/Allowlist/„invite only“ Entscheidung sichtbar machen: a) Wenn System schon User-Tabelle hat: Magic Link nur für existierende User oder Auto-Provisioning per ENV (AUTH_AUTO_PROVISION=1) mit Default=0. b) Wenn kein User-Konzept existiert: implementiere minimale User-Persistenz oder klaren „Invite“-Mechanismus. - Dokumentiere das Verhalten explizit (README/Runbook). 3) Mailversand: - Implementiere/verdrahte Mailer-Konfiguration per ENV (SMTP_HOST/PORT/USER/PASS/FROM oder Provider). - Fallback für lokale Entwicklung: Token wird weiterhin geloggt (aber in Prod optional abschaltbar: AUTH_LOG_MAGIC_TOKEN=0 default). 4) Sicherheit (Minimal-Härtung, kein Overengineering): - Rate-Limit auf Login-Request Endpoint (IP + email), z.B. 5/min und 20/h (werte konfigurierbar via ENV). - Keine User-Enumeration: Response für unbekannte Emails soll gleich aussehen (immer 200/202 "If account exists..."). - Token TTL klar definieren (z.B. 15 Minuten), Single-use, hashed storage falls DB existiert; wenn bereits implementiert, nur prüfen und testen. - Cookie-Settings korrekt: HttpOnly, SameSite (Lax/Strict je nach Flow), Secure bei HTTPS; hinter Proxy korrekt. - CSRF/Origin: State-changing endpoints geschützt; Login endpoints explizit dokumentiert (ggf. Ausnahme). 5) Observability: - Audit-Logs: login.requested, login.consumed, login.failed (ohne Token im Klartext). - Metrik/Logfelder: correlation_id/request_id. 6) Tests: - Unit/Integration Tests für: - public login disabled returns proper error - public login enabled returns 202 + non-enumerating message - rate-limit triggers - token consume works and sets session cookie - proxy headers honored only for trusted proxies 7) Docs/Runbook: - Ergänze docs: "Enable Public Login" mit minimaler .env Vorlage. - Compose-Beispiel: welche ENV Variablen in prod benötigt werden. - Hinweis: Wenn öffentlich erreichbar → Rate-Limit, Mail, TLS, Proxy trust zwingend. Arbeitsanweisung: A) Repo scannen: - finde Auth-Endpoints, Login UI, Env-Konfig, Mailer, Token-Store, Session/Cookies, Proxy-Handling. - nenne gefundene Dateien und relevante Stellen (Pfad + kurze Notiz). B) Implementiere die fehlenden Teile gemäß Zielen. C) Passe docker compose / env.example an: - .env.example oder docs/env.md ergänzen - infra/compose/compose.prod.yml: env vars durchreichen, falls nötig D) Update/erweitere Tests. E) Schreibe PR-Beschreibung: - Was geändert, warum, wie aktivieren, Sicherheitsnotizen, Breaking changes. F) Liefere am Ende: - Liste geänderter Dateien - kurze "How to verify" Schritte (lokal + prod) - offene Punkte/Tradeoffs Wichtig: - Keine Halluzinationen: wenn etwas nicht existiert, sag es und implementiere minimal. - Keine harten Defaults, die Deployment brechen. Alles per ENV schaltbar, sichere Defaults. - Keine externen Abhängigkeiten ohne Begründung. Vermeidbare Fehler (vor dem PR schon sichtbar) „Andere auch“ ohne Auto-Provision/Invite-Policy endet in Chaos: entweder niemand außer dir kommt rein oder jeder kann Accounts erzeugen (Spam). Das muss explizit sein. Token im Log als Dauerlösung ist operational gefährlich. Als Dev-Fallback ok, in Prod optional aus. Rate-Limit vergessen: Public Login wird zur Spam-Schleuder. Risikoabschätzung Security-Risiko: mittel → hoch, wenn öffentlich im Internet ohne Rate-Limit + saubere Mail + TLS. Betriebsrisiko: mittel (ENV/Proxy/Cookie-Konfiguration ist fehleranfällig). Empfohlene Leitplanke: Defaults sicher (public off; auto-provision off; token logging off in prod), Aktivierung bewusst. Verdichtete Essenz Der Prompt zwingt den PR zu: Public Login per ENV, Mailer, Invite/Auto-Provision-Policy, Rate-Limit, keine Enumeration, Proxy/Cookie korrekt, Tests + Runbook. Tiefgründig-ironische Auslassung Public Login ist wie ein offenes Fenster: es macht das Haus „zugänglich“, aber nur dann gemütlich, wenn du auch ein Fliegengitter einbaust. Ungewissheitsursachenanalyse Unsicherheitsgrad: 0.28 Interpolationsgrad: 0.18 Ursachen: Ich kenne aus dem Merge die Flags und den Flow, aber nicht die konkrete Auth-Implementierung (User-Modell, Token-Store, Mailer) in deinem aktuellen Repo-Stand. Der Prompt ist deshalb bewusst als Scan→Implement→Test strukturiert. Leitfragen Kritischer ginge es mit Repo-Snapshot/aktueller Tree-Ansicht: dann würde ich die DoD präziser auf deine Dateien zuschneiden (z. B. exakte Endpoint-Pfade, Session-Stack). Unbeleuchtet: „Public“ nur hinter WireGuard vs. wirklich Internet. Wenn du „Internet-public“ willst, sollten wir zusätzlich Caddy-Layer-Ratelimit + ggf. CAPTCHA/Proof-of-Work diskutieren (minimalinvasiv). Dialektische Erörterung (∴oszillation / ∴resonanz / ∴paradox) These (ein großer PR): Alles auf einmal ist kohärent: Feature-Flag, Mailer, Security, Tests, Doku greifen ineinander. Ein PR = ein mentaler Kontext. Weniger Koordinationskosten, weniger „halber Zustand“. ∴resonanz: passt zum Ziel „es soll einfach funktionieren“. Antithese (mehrere PRs): Public Login ist sicherheitskritisch. Große PRs verwischen Review-Schärfe. Risiko steigt, dass ein Detail (Rate-Limit, Cookie-Flag, Enumeration) untergeht. Außerdem willst du vorankommen, nicht erst nach einer Mammut-Review. ∴oszillation: Geschwindigkeit vs. Kontrollierbarkeit. Synthese (empfohlen): Ja, aufteilen – aber entlang scharfer Grenzen, sodass jeder PR für sich reviewbar, testbar und notfalls revertierbar ist. Keine funktionalen Halblösungen, sondern schichtweise Aktivierung. Empfohlene PR-Aufteilung (minimal, wirkungsvoll) PR 1 – Infrastruktur & Gates (keine Verhaltensänderung) Ziel: Alles vorbereiten, ohne Public Login zu öffnen. Inhalt: ENV-Plumbing (AUTH_PUBLIC_LOGIN, APP_BASE_URL, AUTH_TRUSTED_PROXIES) durchreichen. Feature-Flags im Code verdrahten (Default: off). Doku/Runbook: Wie man Public Login aktiviert (noch nicht aktiv). Tests: „public disabled“ Pfad. Warum zuerst: risikoarm, review-leicht, schafft Fundament. Merge-Risiko: niedrig. PR 2 – Magic-Link Core (kontrolliert, nicht öffentlich) Ziel: Der Flow funktioniert end-to-end für bekannte Nutzer. Inhalt: Magic-Link Request/Consume stabilisieren. Token-TTL, Single-Use, Hashing prüfen/ergänzen. Cookie-Flags + Proxy-Trust korrekt. Keine Enumeration (gleiches Response-Verhalten). Tests für Consume/Session. Warum: Funktionalität vor Öffnung. Merge-Risiko: mittel, aber isoliert. PR 3 – Public Exposure + Abuse-Schutz Ziel: Andere auch – aber mit Geländer. Inhalt: Public Endpoint aktivierbar via AUTH_PUBLIC_LOGIN=1. Rate-Limit (IP + Email), konfigurierbar. Optional: Auto-Provision aus (Default) + Invite/Allowlist-Policy dokumentiert. Audit-Logs (ohne Token-Leaks). Tests für Rate-Limit & Public-Pfad. Warum: Sicherheitsschicht separat reviewen. Merge-Risiko: mittel. PR 4 – Mailer & DX (optional, aber empfehlenswert) Ziel: Weg von Log-Tokens, hin zu echter Zustellung. Inhalt: SMTP/Provider-Config per ENV. Fallback-Logging nur für Dev (AUTH_LOG_MAGIC_TOKEN). Doku + Compose-Beispiele. Warum: Betrieb & UX, nicht Kern-Security. Merge-Risiko: niedrig bis mittel. Entscheidungsheuristik (kurz & ehrlich) Willst du heute rein? → PR 1 + PR 2 zuerst. Willst du andere reinlassen? → PR 3 separat, bewusst mergen. Willst du es „fertig“? → PR 4 danach. Nicht empfohlen: Ein einziger PR, wenn Public Login wirklich öffentlich werden soll. Review-Blindstellen sind dann wahrscheinlich. Vermeidbare Fehler (typisch bei falscher Aufteilung) Public Login aktivieren vor Rate-Limit → unnötige Angriffsfläche. Mailer + Security mischen → Reviews werden oberflächlich. Invite/Auto-Provision ungeklärt → Entweder niemand kommt rein oder jeder. Risikoabschätzung Ein PR: höheres kumulatives Risiko (Security + Betrieb + DX gleichzeitig). Mehrere PRs: geringeres Risiko pro Schritt, bessere Reversibilität. Empfehlung: 4 PRs wie oben; notfalls 3 (PR 4 optional). Verdichtete Essenz Aufteilen ist besser. Baue zuerst das Fundament, dann den Flow, dann öffne die Tür – mit Geländer. Geschwindigkeit entsteht nicht durch Größe, sondern durch sichere Schritte. Tiefgründig-ironische Auslassung Ein großer PR ist wie ein Umzug mit einem einzigen Karton: elegant gedacht, praktisch endet er mit zerbrochenem Geschirr. Ungewissheitsursachenanalyse Unsicherheitsgrad: 0.19 Interpolationsgrad: 0.10 Ursachen: Exakte Repo-Details (User-Modell, Mailer-Status) kenne ich nicht; die Aufteilung ist daher bewusst architektur- und sicherheitsgetrieben statt dateispezifisch. Leitfragen Kritischer? Ja: Wenn du mir sagst, ob „public“ Internet oder WireGuard-only ist, würde ich PR 3 ggf. weiter splitten (Caddy-Rate-Limit vs. App-Rate-Limit). Unbeleuchtet: Monitoring/Alerting auf Login-Missbrauch. Wenn relevant, käme ein PR 5 (Observability-only) dazu. Dialektische Erörterung (∴oszillation / ∴resonanz / ∴paradox) These: Internet-Public Login ist machbar, wenn du es wie ein Produkt behandelst: saubere Defaults, explizite Aktivierung, Abuse-Schutz auf zwei Ebenen (Proxy + App), keine Enumeration, kurze Token-TTL, saubere Cookie/Proxy-Semantik, saubere Logs. Antithese: „Public“ ist nicht „nur Login“ – es ist ein dauerhafter Angriffs-Endpoint. Wenn du das als „nur ein Flag“ behandelst, baust du dir einen Spam- und Brute-Force-Trichter. ∴paradox: je bequemer, desto attraktiver für Missbrauch. Synthese: Vier PRs, aber PR3 splitten wir intern in 3a (Caddy/Edge-Ratelimit) und 3b (App-Ratelimit + Policies), damit du Defense-in-Depth bekommst, ohne einen Megapatch. PR 1 — Infrastruktur & sichere Gates (kein Public Login, keine Behavior-Änderung) Ziel Fundament legen: Env-Variablen, Config-Validierung, Doku-Gerüst, Tests für „disabled“ — ohne Öffnung nach außen. Scope Env vars formal definieren + dokumentieren Konfig-Loader validiert (Fail-fast) nur wenn Feature aktiv Compose/Caddy/Deployment: Variablen durchreichen (ohne Aktivierung) Smoke-Test: public login bleibt disabled Changes (typische Files/Orte; Agent muss im Repo suchen) apps/api/... oder backend/...: config loader (process.env) infra/compose/compose.prod.yml: env passthrough docs/ oder README.md: „Public Login“ Abschnitt Tests: auth.*.test.* Acceptance Criteria UI zeigt weiterhin „Public login disabled“ solange AUTH_PUBLIC_LOGIN!=1 Wenn AUTH_PUBLIC_LOGIN=1 aber APP_BASE_URL fehlt → klarer Startup-Error (oder klarer Runtime-Error), nicht stiller Bug Trusted proxies parsing vorhanden (auch wenn noch nicht kritisch genutzt) PR-Beschreibung (Inhalt) Motivation: „Wir bereiten Public Login vor, aber ändern Default-Verhalten nicht.“ How to verify: ohne Env: request endpoint blockt mit Env unvollständig: klare Fehlermeldung Risiken / Fehlerprävention Keine Breaking Deployments: neue Env vars sind optional, werden nur benötigt wenn Feature aktiviert wird. PR 2 — Magic-Link Core stabilisieren (invite-only/known-user, noch nicht „public“) Ziel Der Flow funktioniert end-to-end sicher: request → token erzeugen → consume → session cookie, aber nur für bestehende Benutzer (oder klarer invite-only Mode). Keine Enumeration. Scope Token-Erzeugung + Store: TTL (z.B. 15 min) single-use (consume invalidiert) Token niemals im Klartext speichern (hash) Consume erstellt Session: Cookie: HttpOnly, Secure (bei https), SameSite=Lax (typisch), Path, Domain korrekt Response-Design gegen Enumeration: request endpoint antwortet immer gleich („If an account exists…“) Audit logs: login.requested (email gehasht oder redacted) login.consumed login.failed (reason codes), niemals token im Log Tests request für existierenden user → 202 + generic msg request für nicht existierenden user → 202 + gleiche msg consume mit gültigem token → session gesetzt, redirect ok consume erneut → 400/401 (single-use) TTL abgelaufen → 401 Doku Klarer Abschnitt: „In PR2: Login nur für bestehende Accounts; provisioning/invite kommt später.“ Acceptance Criteria Du kannst dich zuverlässig einloggen (mit bestehendem User) Kein Token-Leak in Logs (außer expliziter Dev-Fallback, aber den verschieben wir nach PR4) Risiken / Fehlerprävention Cookie/Proxy ist klassischer Stolperstein → PR2 muss diese Pfade testen (mindestens unit/integration). PR 3a — Edge/Proxy Defense: Caddy Rate-Limit + harte Kanten (Internet) Ziel Bevor die App-Endpoints öffentlich werden: Missbrauch schon am Rand abfangen. Scope (Caddy) Rate limit für: POST /api/auth/login/request (oder entsprechender Pfad) optional GET /api/auth/login/consume (leichter, aber request ist wichtiger) Optional: Request size limits, basic bot friction Logging/headers: X-Forwarded-For sauber Forwarded konsistent TLS only: redirect http→https (falls relevant) Umsetzungshinweis (agent muss prüfen, was ihr nutzt) Wenn ihr Caddy direkt konfiguriert: Caddyfile oder infra/caddy/Caddyfile Wenn ihr Plugins nutzt (z.B. rate limit plugin): dokumentieren, wie gebaut wird Wenn kein plugin: Alternative: Cloudflare/Traefik? (wahrscheinlich nein) Notfalls: sehr simples „request body size“ + „per IP conn limits“ (je nach Caddy capabilities) Acceptance Criteria Unter Last (z.B. 50 requests/min von einer IP) liefert Proxy 429 (oder blockt), bevor die App es sieht Normale Nutzung bleibt OK Risiken / Fehlerprävention Rate-Limit im Proxy falsch gesetzt kann echte Nutzer aussperren → konservative Limits + klare Doku. PR 3b — App Defense + Public Exposure + Policy (Internet) Ziel Jetzt erst: AUTH_PUBLIC_LOGIN=1 öffnet public request endpoint — aber mit App-Rate-Limit + Policy. Scope Public enable: AUTH_PUBLIC_LOGIN=1 erlaubt request endpoint ohne bestehende Session AUTH_PUBLIC_LOGIN!=1 blockt mit klarer Fehlermeldung (wie jetzt) Rate limit (App): pro IP (z.B. 5/min burst, 30/h) pro email (z.B. 3/min, 10/h) konfigurierbar per ENV Policy für „andere auch“: Invite-only default: AUTH_AUTO_PROVISION=0 Option: AUTH_AUTO_PROVISION=1 erstellt User beim ersten Magic-Link Request/Consume Optional: Domain allowlist: AUTH_ALLOW_EMAIL_DOMAINS=example.com,foo.de Optional: explicit allowlist file/env: AUTH_ALLOW_EMAILS=... Anti-enumeration: bleibt aus PR2 Observability: Zähler: rate_limited, requests_total, consumes_total structured logs Tests public enabled: request ohne Auth geht 202 public disabled: request ohne Auth geht 403/400 auto-provision off: neue email bekommt zwar 202, aber es wird kein token erzeugt (oder token erzeugt aber consume scheitert) — wichtig: UX klar dokumentieren auto-provision on: neuer user wird erstellt und kann einloggen rate limit: wiederholte requests → 429 Acceptance Criteria Du kannst public login aktivieren und (je nach Policy) auch andere reinlassen Missbrauch ist auf App-Ebene begrenzt, selbst wenn Proxy-Limit versagt Risiken / Fehlerprävention Auto-provision ist riskant (Spam-Accounts) → Default aus, Domain allowlist empfohlen. PR 4 — Mailer + produktionsfähige Zustellung + Dev-Fallback (sauber getrennt) Ziel Magic Links kommen per Mail zuverlässig an. Log-Token nur als Dev-Fallback. Scope SMTP config: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM optional TLS flags Email template: enthält Magic Link mit APP_BASE_URL enthält TTL Info Dev fallback: AUTH_LOG_MAGIC_TOKEN=1 nur in dev; default 0 Tests: mailer unit test (mock transport) integration: bei request wird sendMail aufgerufen Doku / Runbook „Production checklist“: HTTPS APP_BASE_URL korrekt AUTH_TRUSTED_PROXIES gesetzt Proxy rate limit aktiv App rate limit aktiv SMTP validiert Acceptance Criteria Du klickst im UI „Send Magic Link“ und bekommst Mail (Prod) Kein Token im Log in Prod default PR-Erstellungsprompts (je PR separat, ausführlich, agent-ready) Prompt für PR1 Erstelle PR1: "Prepare public login: env gates + docs + disabled-path tests" (no behavior change). Scan repo for auth config loader, env example, compose, docs. Implement: - Define ENV vars (AUTH_PUBLIC_LOGIN, APP_BASE_URL, AUTH_TRUSTED_PROXIES) in config module. - Validation: if AUTH_PUBLIC_LOGIN=1 and APP_BASE_URL missing -> fail fast with clear error. - Ensure compose.prod.yml passes through these env vars (optional). - Add docs section: how to enable later (but note still disabled by default). - Add tests: request endpoint returns "public login disabled" when flag off. Output: list files, verify steps, no enabling in defaults. Prompt für PR2 Erstelle PR2: "Harden magic-link core (invite-only): ttl, single-use, anti-enum, cookie" . Scan for existing token/session/user model. Implement: - Token TTL (15m), single-use, store hashed token (no plaintext). - Consume creates session cookie with HttpOnly, Secure (if https), SameSite=Lax, proper path/domain. - Request endpoint returns generic message for both existing/non-existing emails (no enumeration). - Structured audit logs without token. - Tests: request existing/nonexisting identical response; consume success; consume reuse fails; ttl expiry fails. Docs: clarify invite-only behavior baseline. Prompt für PR3a Erstelle PR3a: "Edge protection: rate limit auth endpoints in Caddy". Find Caddy config (Caddyfile or infra/caddy/* or compose labels). Implement conservative rate limits for POST login request (and optional consume). Document configuration and how to tune limits. Add a small verification guide (curl loop) to observe 429 at edge. Do not change app behavior in this PR. Prompt für PR3b Erstelle PR3b: "Public login enable + app rate limiting + provisioning policy". Implement: - AUTH_PUBLIC_LOGIN=1 enables unauthenticated login request endpoint; otherwise blocked. - App-layer rate limit per IP + per email, ENV configurable. - Policy: default invite-only (AUTH_AUTO_PROVISION=0). If 1, create user on first login. - Optional allowlist: AUTH_ALLOW_EMAIL_DOMAINS / AUTH_ALLOW_EMAILS. - Keep anti-enumeration responses. - Tests: public enable/disable, rate limit, auto-provision on/off, allowlist behavior. Docs: how to safely enable for Internet, recommended allowlist. Prompt für PR4 Erstelle PR4: "Mailer for magic links + production checklist + dev fallback". Implement SMTP mailer with env vars; template includes link (APP_BASE_URL) + ttl. Dev fallback: AUTH_LOG_MAGIC_TOKEN default 0, can be 1 in dev to log link/token. Tests mock mail sending. Docs: production checklist (TLS, base url, trusted proxies, edge+app rate limit, SMTP). Vermeidbare Fehler (Internet-spezifisch, wichtig) Nur App-Rate-Limit ohne Edge-Limit: du frisst trotzdem Bandbreite/CPU. Auto-Provision ohne Allowlist: Spam-Account-Farm. Secure cookie fehlt bei HTTPS: Session kann abfliegen oder unsicher werden. APP_BASE_URL falsch: Login-Link landet auf falscher Origin → „Login geht nicht“, obwohl Backend ok. Risikoabschätzung PR1: niedrig PR2: mittel (Auth/Cookies) PR3a: niedrig-mittel (Proxy-Konfig) PR3b: mittel-hoch (Public Exposure) PR4: mittel (Mail deliverability + secrets) Empfehlung: Reihenfolge strikt einhalten. Public erst nach PR3a+PR3b. Verdichtete Essenz Vier PRs sind richtig; für Internet kommt Defense-in-Depth dazu: PR3a (Caddy) + PR3b (App). Erst Fundament, dann Flow, dann Öffnung, dann Mail. Tiefgründig-ironische Auslassung Wenn du Internet-Public Login baust, baust du nicht nur eine Tür – du eröffnest eine kleine, kostenlose Kundenhotline für Bots. Die rufen nur nie an, sie klicken. Ungewissheitsursachenanalyse Unsicherheitsgrad: 0.27 Interpolationsgrad: 0.20 Ursachen: Ich kenne noch nicht eure exakten Dateipfade, Auth-Implementierung und Caddy-Setup (Plugin/Capabilities). Deshalb sind die PRs als „scan-first“ formuliert und enthalten policy-entscheidende Env-Schalter statt harter Annahmen. Leitfragen Kritischer? Ja: Wenn du mir den aktuellen Caddyfile und den Auth-Endpunktpfad gibst, kann ich PR3a konkret (exakte matcher + plugin choice) statt generisch formulieren. Unbeleuchtet: CAPTCHA/PoW als Bot-Bremse. Für Heimserver meist Overkill, aber bei Internet-Public eventuell sinnvoll, wenn du Spam siehst.