name: Repository Health Check
on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - name: Required files
        run: |
          set -euo pipefail
          req=(".ai-context.yml" "README.md" ".editorconfig")
          missing=0
          for f in "${req[@]}"; do
            [ -f "$f" ] || { echo "::error::Missing required file: $f"; missing=1; }
          done
          # Fail if critical files are missing
          if [ "$missing" -eq 1 ]; then
            echo "Repository health check failed."
            exit 1
          fi
      - name: Basic workflow sanity
        run: |
          set -euo pipefail
          n=$(find .github/workflows -type f \( -name "*.yml" -o -name "*.yaml" \) | wc -l || true)
          if [ "$n" -gt 15 ]; then
            echo "::warning::High workflow count ($n) – consider consolidation"
          fi
