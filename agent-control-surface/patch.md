➕ Neuer PR (bewusst später)
	•	“Output-Forensik” reduzieren, indem WGX-Ausgabeform standardisiert wird (z. B. zwingend --stdout-json oder zwingend Pfad). Das ist eine WGX-Contract-Frage und sollte nicht im ACS-PR eskalieren.
	•	Token persistent machen (Redis/SQLite). Overkill jetzt.

Kurz: Dieses PR mergen, aber vorher 3 Mini-Fixes.
